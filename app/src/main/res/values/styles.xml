<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:numberPickerStyle">@style/MyNumberPickerTheme</item>
    </style>
    <!-- GOOGLE CLOUD VISION-->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Popup Menu style-->
    <style name="PopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textSize">14dp</item>
    </style>
    <style name="MyNumberPickerTheme" parent="@android:style/Widget.Material.NumberPicker">
        <item name="colorControlNormal">#00FFFFFF</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="spotsDialog_custom" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Small</item>
        <item name="DialogTitleText">@string/ocr_message</item>
        <item name="DialogSpotColor">@android:color/holo_blue_dark</item>
        <item name="DialogSpotCount">5</item>
        <item name ="android:background">@drawable/round_corners_white</item>
    </style>
    <style name="spotsDialog_custom_map" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Small</item>
        <item name="DialogTitleText">@string/map_message</item>
        <item name="DialogSpotColor">@android:color/holo_blue_dark</item>
        <item name="DialogSpotCount">5</item>
        <item name ="android:background">@drawable/round_corners_white</item>
    </style>
</resources>
